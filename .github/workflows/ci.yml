name: CI

on:
  push:
    branches: [master, main, pr40-msvc-reproducer]
  pull_request:
    branches: [master, main]

jobs:
  build:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Linux GCC
            os: ubuntu-latest
            configure: cmake -S . -B build
            build: cmake --build build -j$(nproc)
            test: ctest --test-dir build --output-on-failure

          - name: Windows MSVC x64
            os: windows-latest
            configure: cmake -S . -B build -G "Visual Studio 18 2026" -A x64
            build: cmake --build build --config Release --parallel
            test: ctest --test-dir build -C Release --output-on-failure

          - name: macOS Clang
            os: macos-latest
            configure: cmake -S . -B build
            build: cmake --build build -j$(sysctl -n hw.ncpu)

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            cmake \
            libx11-dev libxext-dev libxcursor-dev libxi-dev libxfixes-dev \
            libxrandr-dev libxss-dev libxtst-dev \
            libwayland-dev wayland-protocols \
            libegl1-mesa-dev libgl1-mesa-dev libgles2-mesa-dev

      - name: Configure CMake
        run: ${{ matrix.configure }}

      - name: Build
        run: ${{ matrix.build }}

      - name: Upload Windows font reproducer
        if: runner.os == 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: pr40-msvc-font-reproducer
          path: |
            build/Release/f15se2-ex.exe
            build/_deps/sdl3-build/Release/SDL3.dll

      - name: Check unoptimized MSVC font rasterizer
        if: runner.os == 'Windows'
        continue-on-error: true
        run: |
          cmake --build build --config Debug --parallel
          ctest --test-dir build -C Debug -R gfx_render_behavior_tests --output-on-failure

      - name: Test
        run: ${{ matrix.test }}

  coverage:
    name: Coverage
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            cmake gcovr \
            libx11-dev libxext-dev libxcursor-dev libxi-dev libxfixes-dev \
            libxrandr-dev libxss-dev libxtst-dev \
            libwayland-dev wayland-protocols \
            libegl1-mesa-dev libgl1-mesa-dev libgles2-mesa-dev

      - name: Configure CMake (coverage)
        run: cmake -S . -B build -DCOVERAGE=ON

      - name: Build
        run: cmake --build build -j$(nproc)

      - name: Run tests + generate coverage report
        run: cmake --build build --target coverage

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: build/coverage.xml
          # Public repo: tokenless upload is fine. Never fail CI if the upload
          # is rate-limited or the service is down.
          fail_ci_if_error: false
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
